%% Using ReLU as the activation function and keeping the general CNN assumptions valid 

Signals are scaled to 0 - 1  % maybe try normalizing also 

Weights are initialized very low both positive and negative

Ground truth is either 0 or 1 ; 

Bias isn't used yet. 